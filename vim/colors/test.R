library('active_support')

CONSTANT <- 'FOLLOW'

initialize <- function(attributes = NULL) {
  colorscheme <- list(
    'a' = 1,
    'b' = 2,
    'c' = TRUE,
    'd' = NA
  )
}

vector <- c('a', 'b', 'c')

